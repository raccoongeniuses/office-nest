<!DOCTYPE html>
<html class="light">

<head>
    <meta charset="utf-8" />
    <title>Leaderboard - Office Nest</title>
    <link href="/css/styles.css" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
</head>

<body
    class="bg-stone-50 dark:bg-nest-900 min-h-screen font-mono text-stone-900 dark:text-vibe-100 transition-colors duration-300">
    <nav class="bg-white dark:bg-nest-800 border-b-2 border-stone-800 dark:border-stone-700 p-4 mb-6">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold uppercase tracking-wide dark:text-vibe-500">Office Nest</h1>
            <a href="/dashboard" class="text-sm font-bold hover:underline">Back to Dashboard</a>
        </div>
    </nav>

    <main class="container mx-auto px-4 max-w-2xl">
        <h2 class="text-3xl font-bold uppercase mb-6 dark:text-vibe-yellow">Top Contributors</h2>
        <div class="bg-white dark:bg-nest-800 border-2 border-stone-800 dark:border-stone-700 rounded-sm shadow-[4px_4px_0px_0px_rgba(41,37,36,1)]">
            <table class="w-full text-left border-collapse">
                <thead>
                    <tr class="border-b-2 border-stone-800 dark:border-stone-700 bg-stone-100 dark:bg-nest-700">
                        <th class="p-3 uppercase text-sm">Rank</th>
                        <th class="p-3 uppercase text-sm">User</th>
                        <th class="p-3 uppercase text-sm">Level</th>
                        <th class="p-3 uppercase text-sm text-right">XP</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each leaderboard}}
                    <tr class="border-b border-stone-200 dark:border-stone-700 last:border-0 hover:bg-stone-50 dark:hover:bg-nest-700">
                        <td class="p-3 font-bold">#{{inc @index}}</td>
                        <td class="p-3">
                            <div class="font-bold">{{this.name}}</div>
                            <div class="text-xs text-stone-500 dark:text-vibe-400">{{this.currentStreak}} Day Streak 🔥</div>
                        </td>
                        <td class="p-3"><span class="bg-stone-200 dark:bg-stone-600 px-2 py-1 rounded text-xs font-bold">Lvl {{this.level}}</span></td>
                        <td class="p-3 text-right font-bold text-indigo-600 dark:text-vibe-cyan">{{this.xp}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </main>
    <script>
        lucide.createIcons();
    </script>
</body>

</html>
